document.getElementById("contact-form").addEventListener("submit", function (e) {
    e.preventDefault();

    // Данные формы
    let name = document.getElementById("name").value;
    let email = document.getElementById("email").value;
    let message = document.getElementById("message").value;

    // Ваш Telegram токен и ID чата
    const TELEGRAM_BOT_TOKEN = "7611413032:AAENIQwno0Amc4iPVnGMmehCeNJG_JH9aVM";
    const TELEGRAM_CHAT_ID = "983197657";
    
    let text = `📩 Новое сообщение с сайта:
🔹 Имя: ${name}
🔹 Email: ${email}
📝 Сообщение: ${message}`;

    let url = `https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage?chat_id=${TELEGRAM_CHAT_ID}&text=${encodeURIComponent(text)}`;

    fetch(url)
        .then(response => {
            if (response.ok) {
                document.getElementById("result-message").style.display = "block";
                document.getElementById("contact-form").reset();
            } else {
                alert("Ошибка отправки сообщения.");
            }
        })
        .catch(error => alert("Ошибка: " + error));
});
